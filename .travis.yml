language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sudo-application' ]; then cd .. && eval "mv $currentfolder generator-sudo-application" && cd generator-sudo-application; fi
